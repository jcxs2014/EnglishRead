# 新书精读启动指令模板

> 用途：每本新书开工时，复制下方模板填空后发给 agent。
> 重要：本文档是执行规则的核心入口——**每本新书都要先读本文档**，不得跳过。
> 依据：2026-08-27~09-07 全库 30+ 书审计 + 2026-09-05/06/07 三批跨书互证固化。
> 位置：本文档保存于 `docs/新书启动模板.md`。

---

## 一句话启动指令

> **用法**：每本新书开工时，复制下方对应模板填空后发给 agent。
> **关键**：本指令 = 执行规则的核心入口——**每本新书都要先读本文档**，不得跳过。
> **依据**：2026-08-27~09-07 全库 30+ 书审计 + 2026-09-05/06/07 三批跨书互证固化。
> **位置**：本文档保存于 `docs/新书启动模板.md`。

**执行方（精读）**：
```
请先读取 docs/新书启动模板.md，然后按其中的规则执行新书精读。开工前额外读取 AGENTS.md（执行规则）和 COLLABORATION.md（并行实例消息），避免重复他人已完成的任务。
```

**审查方（验收）**：
```
请先读取 docs/新书启动模板.md，然后按其中的"独立审查五步法"对《书名》进行审查，报告缺陷清单。若书目录不存在或精读尚未完成，先告知用户而非直接开始。
```

---

## 执行规则（每步必含，不得跳过）

```
精读新书：《书名》（作者），epub 在 notes/books/<slug>/library/

⚠️ 执行规则（每步必含，不得跳过）

0. 任务规划（首条，不得跳过）：
   - 创建 TodoWrite 任务表，列出自 ch01 至 chNN 全章节，
     每条含：章节号 + 简短标题 + 状态（pending/in_progress/completed）
   - 按"三章一批"原则分组（ch01-03 / ch04-06 / ...）
   - 每批 commit 后更新对应条目的 commit hash
   - 上下文压缩后，优先读 TodoWrite 恢复进度，同时重新读取 docs/新书启动模板.md 恢复规则上下文，禁止凭压缩前的记忆继续

1. 原文先行：先跑 extract_chapters.py 提取 text/，把章节清单
   （含跳过的书目页/版权页/目录页）发我确认后再动笔

2. 体裁与格式：这是<言情长篇/短篇合集/推理小说/非虚构>，
   按 AGENTS.md 对应格式执行；不确定就先问我

3. 首章试产：先只写 ch01
   - 必须跑 verify_quotes + check_vocab + check_entities
   - 报告必须附工具原始输出（verify_quotes tail -5 + check_vocab FAIL行 + check_entities结果）
   - 我验收格式，通过后你再批量推进

4. 批次节奏：三章一批，每批独立 commit
   - 每批完成后必须先贴工具输出，不贴不继续
   - commit 前三件套全绿（verify_quotes / check_vocab / check_entities）
   - 有 text/ 的批次加跑 check_chapter_quotes
   - **漏提交检测（强制）**：每批 commit 后立即跑 `git ls-files <书目录> | wc -l` 与 `git status --short <书目录>` 比对该批应提交数；若有 md 仍显示 `??`（untracked）即漏提交，必须补 `git add` + commit——Aickman 批次 ch11 在批4 未被纳入提交、直到全书收尾才以 untracked 暴露，属漏提交盲区

5. 词汇逐条验证（第 8b 条核心）：
   - 每个词条写入前必须 grep 验证："grep -i 'word' text/chNN.txt" 有输出才写
     ——**以本章 text/ 为准，跨篇词条不允许写入**（词在全书他章出现≠本章可收；
      若确需保留，check_vocab 会以 WARN 报"词条跨篇"），例句同理取本章原句
   - 例句直接从原文复制，不改写
   - **禁止添加"（未出现在原文）"等标注绕过工具**
   - check_vocab 报 FAIL=0 不代表词汇干净——工具只检词频，不识别中文标注
   - 2026-08-30 Barron's 批次实证：52篇928条 A 类虚构靠"（未出现现在原文）"标注绕过工具检测

6. 引语修复同步（第 9 条）：
   - 引语换新句时分析必须同步重写，禁止改引语留旧分析
   - 关键词必须能在引语中找到（允许词形变化）
   - 孤儿块（有分析无引语）和重复块均属缺陷
   - **中文理解/上下文/呼应等全部子项须随引文同步改写**——Aickman ch01 改引文后中文仍写「完全……」、ch06 截断虚构尾巴后中文仍写「房中之房」，均属「改引语留旧分析」的变体，审查时按缺陷处理

7. 总览最后写：概述/金句/情感节点完成后，
   交叉核对人物身份/关系/结局/叙事结构，引语逐句 grep 验证

   ⚠️ 总览引语核验是独立门禁（verify_quotes 不覆盖 00_*.md）：
   - verify_quotes 167/167 全绿不等于总览层干净——它是章节级门禁，总览是独立盲区
   - 金句精选/情感节点/概述的每条引语必须逐字 grep epub 验证，不可省略
   - 本次 Aickman 批次实证：verify 全绿但金句精选仍含 6 处改写/虚构（末尾虚构/跨标签拼接/虚构衔接）

   ⚠️ **说话人归属核验（新增，Room 37% 误归教训）**：
   - verify_quotes 全绿 = 引语逐字正确，但说话人/场景可能完全归错
   - Room 金句精选 30 条中 11 条（37%）人物误归：Julia 说的话归给 Astrid，Rudbeck 的话归给 Jonny，Kim 的话归给 Julia
   - 根因：上下文分析/重要性/为什么这样写三子项凭记忆填充，引语本身真实所以 verify 通过
   - **验证方法**：每条引语必须 `grep -n -B10 -A5 "引语关键词" text/chNN_NN.txt` 确认说话人；
     涉及多方的（如 Kim 对 Julia / Kim 对 Rudbeck / Rudbeck 对 Kim）必须 grep 上下文 200 字符窗口二次确认
   - 特别危险：同一人对多人说话（Kim 对 Julia / Kim 对 Rudbeck 语气相似）、多视角叙事中同一描述不同人物；
     凡涉及人物转换的引语，归属置信度必须达到"看到说话人标签"才算数

   ⚠️ **审查报告缺陷须经「读文件定位→grep 原文」二次确认**：
   - 审查方给出的引文偏差（如「bright ones→bright girls」）常只是表象——执行方接到后必须回原文 grep 整句，可能发现更深的虚构衔接（Aickman ch04 报告只指「bright ones」，实际后半句「the ones who might have gone to university」在 epub 完全不存在，属编造衔接）
   - 执行方不得照抄报告结论即改，须独立核实整句；核实后若发现报告未覆盖的额外偏差，一并修复
   - 审查报告编号（⑩/⑬）是审查方自编号，不对应文件章节号——定位缺陷先读文件再 grep，禁止套用报告暗示的映射（Aickman 报告 ⑩=ch01、⑬=ch04，文件里是 ch10/ch13）
   - Room ㉗㉘ "No pain, no gain" / "We should settle this" 在 text/ 完全不存在，
     但 verify_quotes 报 235/235 全绿——直到对 00_*.md 引语做 text/ 全量扫描才发现
     （再次印证上方"总览引语核验是独立门禁"——同一条规则的两种实证）
   - **总览引语必须逐条 grep text/ 验证**，不可跳过；MISS 未必是工具假 MISS（Room 实战两条真实虚构）

   ⚠️ 引文截断与拼接红线：
   - 省略号 `…` 左侧必须是原文原词，右侧不得添加原文不存在的词句
     （例："…four shapely chandeliers; and a room within the room" ← "and a room within the room" 是原文不存在的虚构尾巴）
   - 跨叙述标签拼接禁止：前半句 `"It is necessary that he should come regularly."` + 后半句 `Ursula continued. "Necessary for the clocks."` 不可合并为一句——两段属不同说话标签
   - 虚构衔接禁止：用原文不存在的 bridging 句连接两段真实引文（如 "bright girls…，the ones who might have gone to university" —— "the ones who…" 在 epub 不存在）

   ⚠️ 工具 MISS ≠ 虚构：
   - normalize 拼接脚本对含特殊字符（' / " / ’）或超长句有假 MISS
   - 当工具报 MISS 但逐篇 `find()` 在 epub 原文中能匹配到时，以逐篇 find 为准
   - 可靠验证顺序：逐篇 find（原书）> normalize 拼接 > 报告数字

8. 内联 Gate（每章写完即跑，不等全书完成）：
   - 每章精读完成后立即跑 verify_quotes + check_vocab + check_entities + check_chapter_quotes（有 text/ 时）
   - **FAIL=0 才推进下一章**——不允许"先生成后检查"（The Giver 批次凭记忆补引文导致 25 条 A 类虚构的根因）
   - 有 text/ 的批次**必须加跑 check_chapter_quotes**（AGENTS.md §9e：**凡有 text/ 的书一律加跑**，不设"100 Great 专用"例外）

9. 全书完成后：按"独立审查五步法"做最终验收审查，报告三件套数字，等指令再 push

   ⚠️ **总览引语门禁（verify_overview_quotes）**：
   - 总览文件（概述/金句精选/情感节点）不在 verify_quotes 主口径内，是独立盲区
   - Nine Women One Dress 审查实证：金句精选 10/10 全绿，但情感节点㉔ + 金句精选㉔ 引语截断（"the mother of my child" 被省略）未被检测
   - **每批总览完成后**，立即跑 `python3 scripts/verify_overview_quotes.py "<书目录>" "<epub>"`
   - 报告必须附原始输出；FAIL（非 0 退出码）须修复后重新通过才可 commit
   - 通过门禁后再按独立审查五步法做最终验收

### 🕐 门禁时序表（何时跑什么，2026-09-06 固化）

| 时机 | 必跑 | 常见错误 |
|------|------|---------|
| 开工第 1 步 | `extract_chapters.py` | 件数≠章数；短章被 min-len 滤掉未补提 |
| 首章试产（第 3 条） | verify_quotes + check_vocab + check_entities + check_chapter_quotes | 报告未附原始输出即继续；言情格式下 0/0 盲区（已修） |
| 每章写完（第 8 条内联 Gate） | 同上四件套 | grep 链让 FAIL 行穿过 `&&`，未等绿灯就 commit |
| 每批 commit 前（第 4 条） | 四件套 + 漏提交检测 | 漏提交检测未跑；git add -A 裹挟他人文件 |
| 写完总览（第 7/9 条） | `verify_overview_quotes.py` | 概述行内英文引语漏 grep；金句超 25 条致 CIRCLED 漏检 |
| 全书终验（第 9 条五步审查） | 五步法 + `audit_book.py` + `check_crossref.py` | crossref 报警未经人工复核直接判缺陷 |
| 任何时候 check_vocab 报 A类虚构 | 先查词条是否含撇号 | 不裁决直接删词条（A/B 裁决规范见第 5 条） |

**五步法与工具对应**：a 三件套重跑（verify_quotes/check_vocab/check_entities）→ b 逐章归属（check_chapter_quotes）→ c 结构扫描（grep '^> ' 口径）→ d 语义二审（关键词锚定 + crossref 抽查 + 带反例子代理）→ e 总览核对（verify_overview_quotes + 说话人窗口 + 行内引语人工 grep）。


⚠️ 报告用语红线（违反则本批次返工）：
- 禁止说"验证通过"、"全部绿了"、"commit 完成"——每条完成声明必须附原始工具输出
- 禁止只贴数字（"FAIL=0"、"WARN=3"）而不附原始输出
- 禁止加"（未出现在原文）"标注——工具不识别它是 FAIL，此类标注是 A 类虚构信号
```

---

## 协作板身份与时间戳规范（新增，2026-08-31 实证）

**身份规范**：
- 每个 IDE 实例有固定身份（ZCode-Mac / Opencode-Mac 等），发帖前确认自己是哪个
- **不得修改其他 agent 的消息**——即便发现错误，也只能在自己的新消息里补充说明，不能改动他人已发布的内容
- 交接时须核对 COLLABORATION.md 最新消息的身份前缀，避免串改

**时间戳规范**：
- 发帖前必须运行 `date -u '+%Y-%m-%d %H:%M UTC'` 查实当前 UTC 时间
- 不得估算、记忆、捏造时间戳；时间错误会使协作板排序失效

**原因**：2026-08-31 本会话 ZCode-Mac 误将 Opencode-Mac 的合法消息改为己出，并捏造时间戳 11:20 UTC（实际 10:59 UTC），经用户指出才更正。

---

## 强制工具输出示例

每批完成后报告格式：
```
=== chXX-chYY 批次完成 ===

verify_quotes：
$ python3 scripts/verify_quotes.py "<书目录>" "<epub>" 2>&1 | tail -5
[粘贴工具原始输出]

check_vocab：
$ python3 scripts/check_vocab.py "<书目录>" 2>&1 | grep -E "FAIL|WARN"
[粘贴工具原始输出]

check_entities：
$ python3 scripts/check_entities.py "<书目录>" 2>&1
[粘贴工具原始输出]

commit: [commit hash]
```

---

## 可选补充项（按书情况追加）

| 情况 | 补充指令 |
|------|---------|
| 多卷 / 多 POV | "双视角，每章标注 POV" |
| 有特定学习关注点 | "我主要想学它的<对话/描写/论证>，分析侧重这里" |
| epub 章节合并/拆分异常 | "提取后打印章节清单，编号映射先跟我对齐" |
| 敏感内容风险 | 依赖 AGENTS.md MiniMax 1027 规则，无需额外指令 |
| 续作 / 同作者第二本 | "格式与《前一本》保持一致，对照其文件结构" |

---

## 每条规则挡住的历史坑（字典用法：通读一遍建立地图，之后按问题 grep 关键词定位）

> **怎么用这张表**：不必背。开工时通读一次知道"有什么类型的坑"；执行中遇到具体疑问（如"总览引语要不要逐条 grep"）再 grep 关键词回到本表看实证。已发生的规则冲突以正文条款为准，本表只负责解释"为什么有这条规则"。

### 分组索引

- **开工对齐类**（对应第 1-3 条）：章节清单确认、体裁声明、首章试产
- **批次节奏类**（对应第 4、8 条）：三章一批、内联 Gate、漏提交检测
- **引语质量类**（对应第 6、9 条）：修复同步、截断/拼接/衔接红线、假 MISS
- **总览与审查类**（对应第 7、10 条）：总览独立门禁、说话人归属、报告二次确认
- **词汇表类**（对应第 5 条）：逐条验证、分档、占位符


| 条目 | 对应事故（实证） |
|------|----------------|
| 1 章节清单确认 | Venus Fly Trap 文件号与 Chapter 号差 1；TWF ch29/31 映射混乱；Eleventh Hour front matter 取舍；The Giver epub 23 章合并为 3 个 HTML |
| 2 体裁显式声明 | Book Lovers 言情用逐句格式写到 35 篇崩坏重做 |
| 3 首章试产 | 所有"后期质量衰减"的第一现场都是首章未对齐；ch01 拦住 = 一章返工，ch30 才发现 = 三十章 |
| 4 三章一批 | Book Lovers / 100 Great 两次大量生成后期衰减实证 |
| 5 词汇逐条验证 + 禁止标注 | 2026-08-30 Barron's 批次：52篇928条"（未出现在原文）"A 类虚构——check_vocab FAIL=0 报绿，实际全错 |
| 6 引语修复同步（含中文理解） | 以为只改引语行即可 | Aickman ch01 改引文后中文仍写「完全……」、ch06 截断虚构尾巴后中文仍写「房中之房」，均属「改引语留旧分析」变体 |
| 4 漏提交检测 | 以为批次 commit 一定含全部文件 | Aickman ch11 在批4 未被 `git add` 纳入，全书收尾才以 untracked 暴露，属漏提交盲区 |
| 7 审查报告缺陷二次确认 | 以为报告指哪改哪即可 | Aickman ch04 报告只指「bright ones」，执行方 grep 整句才发现后半「the ones who might have gone to university」是 epub 不存在的虚构衔接 |
| 7 总览延后 + 交叉核对 | Angelic Death 概述人物身份/关系/结局系统性写错 |
| 7 总览引语独立门禁 | Aickman verify 167/167 全绿，金句精选仍含 6 处改写/虚构（末尾虚构/跨标签拼接/虚构衔接）——verify_quotes 不覆盖 00_*.md |
| 7 引文截断红线 | Aickman ch06 金句截断处添加 "and a room within the room"（原文是 "a baronial fireplace"），属末尾虚构 |
| 7 跨标签拼接禁止 | Aickman ch11 金句合并 "He should come regularly." + "Necessary for the clocks." 属跨标签拼接 |
| 7 虚构衔接禁止 | Aickman ch04 "bright girls…，the ones who might have gone to university" 中 "the ones…" 在 epub 不存在，属虚构 bridging |
| 7 工具假 MISS | normalize 拼接脚本对含 ' 或超长句有系统性假 MISS，逐篇 find() 更可靠 |
| 7 说话人归属必须验证 | verify_quotes 全绿 ≠ 说话人正确；Room 实战：㉒ "What I did to you" 逐字命中但说话人 Rudbeck→Kim 错位；金句精选30条中11条（37%）人物误归（Julia→Astrida/Jonny→Rudbeck/Kimmer→Kim 等），引语真实但上下文分析凭记忆填充导致角色互换 |
| 7 虚构引语发现机制 | verify_quotes 不覆盖 00_*.md（总览三篇）；Room ㉗㉘ "No pain no gain" / "We should settle this" 在 text/ 完全不存在，但 verify 报全绿；最终靠脚本全量扫描引语到 text/ 才触发 MISS |
| 审查报告编号非文件编号 | 审查方的 ⑩/⑬ 是自编号，可能对应 ch01/ch04，须先读文件定位再 grep 原文确认 |
| 编号体系混淆 | Aickman 报告 ⑩=ch01、⑬=ch04（文件里是 ch10/ch13）——三套编号必须严格区分，禁止混用 |
| verify_quotes 不覆盖总览 | Aickman verify 167/167 全绿，金句精选仍含 6 处改写/虚构 |
| 末尾虚构 gilding | Aickman ch06 "…four shapely chandeliers; and a room within the room" —— "and a room within the room" 在 epub 是 "a baronial fireplace" |
| 跨标签拼接禁止 | Aickman ch11 金句合并 "He should come regularly." + "Necessary for the clocks." 属跨标签拼接 |
| 虚构衔接禁止 | Aickman ch04 "bright girls…，the ones who might have gone to university" —— "the ones…" 在 epub 不存在 |
| 8 内联 Gate | The Giver 批次 25 条 A 类虚构——内联 Gate 把"生成后检查"变为"边生成边验证" |
| 9 五步法验收 + 等指令 push | push 策略：批次定稿后统一推送 |
| ⚠️ 报告用语红线 | Astonishing ch51-ch75 只写文件不跑工具，直接报"全部绿了"——无工具输出 = 无完成声明 |
| 协作板身份/时间戳 | ZCode-Mac 改 Opencode-Mac 合法消息 + 捏造时间戳 11:20 UTC（实际 10:59）；须确认身份后再发帖，时间必须 `date -u` 查实 |
| 诗歌回忆录格式 | Ten Bridges：现有格式表无"诗歌"档，需按"随笔集逐篇精读"框架适配，新增"诗歌技法专项"章节 |
| 跨行短语 WARN | Ten Bridges：check_vocab 对跨行短语（如 "olive trees and oracles"）有系统性 WARN，需逐词 grep 裁决 |
| 分析层 cross-ref 数字必须 grep | Book of Doors：ch16-6"ch13 杀5-7猎书人总数17"纯属编造（17 实为馆藏总数）——verify 全绿不覆盖分析层 |
| 多句/破折号引语 false MISS | Getaway Girl：verify_quotes 对含 `—` 的引语有系统性 false MISS，应拆为单句格式 |
| 长章单独成批 | Language City：100k+ 字符长章单独一批，有效控制质量衰减 |
| "删了但留标记行"残留 | Language City：`\| smuggled | … |（ch13 用法，删）\|` 以 WARN 形态存活——应整行删除 |
| 无章节号中篇按场景节分章 | Butcher of the Forest：无章节号的中篇可按 `* * *` 场景分隔分章 |
| 短篇合集关键词锚定 | Secret Lives：关键词必须逐字命中引语（本次 5 处违规） |
| 词汇表每章 20-30 词条 | Addie LaRue：每章词条数控制在 20-30 为佳，过多导致 WARN 泛滥 |
| 文件命名须 chNN 前缀 | Ten Bridges：精读文件须用 `ch<NN> <title>.md` 格式，与其他书统一 |
| check_vocab 逐章验证 | 以为 FAIL=0 够用 | Ligotti ch33 nullify/ch52占位符/ch73 night 在别章出现即过 FAIL；必须逐章 grep 验证例句 |
| 旧格式圈数字转换 | 以为自动编号正确 | Ligotti 49 章转换后 ch22/ch27/ch28/ch29 全部残留 `原句1`；转换后必须检查编号连续性 |
| check_chapter_quotes 假 MISS | 以为 MISS 都是引语虚构 | Ligotti ch22 PLACE heading 假 MISS（原文有 whose，flat 匹配丢失）；以人工 grep 为准 |

### 2026-09-05/06 六本完工批次新增坑（跨书互证的固化为规则）

| 条目 | 对应事故（实证） |
|------|----------------|
| 无编号言情格式须自备脚本 | Up in Molten Lights：verify_quotes/check_chapter_quotes 对 `> "..."` 无编号格式抽到 0/0（工具只认 `> **原句 N:**`）——言情格式书的引语门禁必须自备 flat 分段脚本（按引号分段，每段 ≥20 flat 字符比对本章 text/） |
| <20 字符引语被静默跳过（互证） | Perfection 2 处 + Forest of Scars 5 处 + Rookie Season 11 处：工具不报错但实际未检——写完即对比"grep 引语行数 vs 工具提取数"，不一致即信号，差值逐条人工 grep |
| check_vocab 撇号缩写词条误报 | Up in Molten Lights ch54：I've/he'd 类词条去撇号后 <4 字符必判 A类虚构——实为工具误报，词条真实存在；凡 FAIL 先核词条是否含撇号 |
| check_vocab 例句起点锚定 | Helm：例句起点落在页码污染点或省略主语时报假 FAIL——例句起点改取原文后缀片段即可过（后缀仍是逐字） |
| 跨标签拼接是最高频引语缺陷（互证 7 书） | Aickman/Taken/Forest of Scars/Color of Death/Helm/Up in Molten Lights：两句台词或"台词+叙述"用 `...` 连接全部违规——引语块内多段必须逐段独立验证，或整块替换为原文连续片段 |
| 分析层 cross-ref 章号必须当场 grep | Forest of Scars 16/21 缺陷源 + Life and Death and Giants 10 处：写"chNN 的 XX"凭印象必错——正则抓 `chNN "quoted"` 对 flat 比对所指章；报警须人工读行防同行多引用误配 |
| 数字断言必须实测 | Perfection 约 20 处词数口误（"九词"实 11）+ Forest of Scars 4 处：写"几个词/多少条"前当场数 |
| 跨章连读指涉须 grep 定章 | Perfection：koine 在 ch07 非 ch06——"与上一章连读"不能凭印象写 |
| 词条头必须用本章原词形（互证） | Color of Death（torn→tore）+ No Take Backs（cackle→cackling）+ Black River（-e 结尾动词）：check_vocab 按前后缀匹配，词形变化即报跨篇/虚构 |
| 词汇表批量生成勿留占位行 | Up in Molten Lights：`\| xx→ \| — \| — \|` 占位行必 FAIL；大批次末尾占位符/粗体不闭合是注意力衰减签名，写完 grep 行尾 `\| *\$` 自查 |
| 词汇表单章膨胀即质量警报 | Up in Molten Lights ch49-54：单章 500-900 行（正常 20-30 词条）= 后期崩坏签名——写完每章自查词条行数 |
| 词汇表移动词条后必须去重 | Up in Molten Lights：跨档移动 40+ 行后暴露 68 行累积重复（ch48 一章 57 行）——批量移动行后须按"词头已存在于目标档则删源行"去重 |
| extract_chapters 短章/极短章 | Wild Dark Shore（ch33=195c 被 600 阈值滤掉，需 --min-len 200）+ No Take Backs（Prologue 61c 被跳过需人工补提）+ Helm（8 个短插曲节被跳过）——提取后必核"提取件数 vs 书内章数"，短章人工补提 |
| text/ 重命名必须用精确映射表 | No Take Backs：`replace("ch0",...)` 子串替换把文件名后缀里的 ch0 也改了——重命名一律用 old→new 字典映射 |
| 非正文章节勿占 ch 编号 | Butterfly Girl：版权页占 ch01 与正文冲突，check_vocab 的 glob 同编号互相覆盖——非正文重命名为 xx_copyright 等非 ch 前缀 |
| check_entities 分析层规避法（互证） | Helm（WhatsApp/PTSD/未来词）+ Taken/Rookie Season（PTSD/Twilight/Tropes 英文标签）+ Book of Heartbreak（导航英文）+ Wild Dark Shore（Enemies to lovers）+ No Take Backs（46 条 whitelist）——导航/分析层一律用中文措辞（"应激反应"代 PTSD），确需保留的建 whitelist.txt |
| 并行实例 commit 裹挟（高频复发，互证 8 起） | Rookie Season ×2 + Memories Like Fangs ×2 + Perfection + Afterdark + No Take Backs + Up in Molten Lights（他实例抢先 commit 我方文件）——一律 `git add <明确路径> && git commit -m msg -- <路径>` pathspec 原子直提；commit 前 `git diff --cached --name-only` 查 index；发现被裹挟：不改写他人 commit，核实内容完整 + 协作板双向报备 |
| 引语行格式统一 | Memories Like Fangs：`> **原句 1: "quote"**`（bold 包全行）被结构扫描漏检——统一 `> **原句 1:** "quote"`；修复脚本禁止 re.S 跨块替换（曾损毁 116 处，git 回滚重来） |
| 总览引语编号 ≤25/文件 | Black River：金句 ㉖-㉚ 超出 verify_overview CIRCLED 口径（止于㉕）静默漏检——每篇总览引语控制在 25 条内或改用人工脚本兜底 |
| 总览内联英文短语同样逐条 grep | Rookie Season：概述 44 个内联短语抓出 2 处非连续转述——工具不覆盖的行内引语是盲区，写入前逐条 flat 直查 |
| 分析层虚构穿透全绿门禁 | Perfection ch11 "iftar Books"（全书查无的乱码实体）存活于全绿之下——语义子代理（带反例+防幻觉条款）才抓到 |
| commit 前看完整工具输出 | Memories Like Fangs：grep 链让 FAIL 行穿过 `&&`，三次"未等绿灯就 commit"——提交声明必须附完整原始输出而非 grep 尾部 |
| check_vocab 误报 `\|` 分隔行 | Memories Like Fangs：概述/作者行用 `\|` 作分隔符（如 `作者 \| 出处`）被 check_vocab 判为词汇表行报 FAIL——概述/导航层避免用 `\|` 分隔，改用 `·` |
| 关键词锚定自建检查器（≥20 章的书推荐） | Memories Like Fangs：规则 9b 靠人工核在大批次必然漏——自建锚定检查器（关键词英文词须命中引语或"为什么这样写"文本，允许词形变化，剔除 stop words）一次扫 237 块抓出 59+ 处违规；Color of Death 审查同类 25 报→4 真违规 |
| git index.lock 并发处理 | Memories Like Fangs + Rookie Season：多实例并行遇 `index.lock`——**等待重试，禁止强删锁文件**（强删会损坏他人正在进行的提交） |
| check_crossref 输出解读 | 新工具（2026-09-06）：报警格式 `文件:行号 → chNN 查无「片段」`；两类常见误配须人工复核后才能判缺陷——①同一行多个 chNN 引用与最近引语错误配对；②跨章回忆式转述（分析层合法提及他章内容但措辞非逐字）。报警≠缺陷，缺陷=读行确认后引语确属虚构/错章 |
| commit 前 pull+status 确认工作树归属 | Up in Molten Lights：ch76-79 批次被他实例以我工作树文件抢先 commit（9fc3247），自己的 add 落空——commit 前 `git pull` + `git status` 确认工作树文件未被他人提交；被抢先时核对内容完整性再决定补 commit 或放弃 |

---

## 词汇 A/B 类裁决规范（第 5 条核心）

check_vocab 报 FAIL 时，先以 epub 展平全文终极裁决：

| 类别 | 判断标准 | 处理方式 |
|------|---------|---------|
| **A 类：真虚构** | epub 展平全文也查无此词 | 用文中真实词替换，禁止删除了事 |
| **B 类：语料缺失** | epub 有但 text/ 提取件缺 | 优先改用文中真实词形，或重跑 extract_chapters.py 修复覆盖 |

**禁止**：不裁决直接删词条；不裁决直接加"（未出现在原文）"标注了事。

---

## 编号规则与防混淆规范（2026-08-31 新增）

本系统存在 **三套独立编号体系**，必须严格区分，禁止混用：

### 三套编号体系

| 编号体系 | 格式 | 作用域 | 重置规则 | 示例 |
|---------|------|--------|---------|------|
| **引文块编号** | `> **原句 N:**` | 同一精读文件内 | 同一篇内连续不重置 | ch01 中 `原句 1` → `原句 2` → … |
| **金句精选编号** | `⑩`（圆圈数字） | 全书金句精选.md | **不重置**，全书排序 | ㉑、㉒、㉓ … |
| **情感节点编号** | `节点一` / `节点 N` | 全书情感节点.md | **不重置**，全书排序 | 节点一、节点二 … |
| **审查方自编号** | `⑩` / `⑬` | 仅在 COLLABORATION.md 审查报告中 | 临时，**非文件编号** | 仅用于定位缺陷，不等于 ch10/ch13 |

### 关键约束

1. **引文块编号 ≠ 金句编号**：同一篇 story 的引文块编号从 1 开始，金句编号按全书排序。例如 ch01 引文块 ③，不等于金句③。
2. **金句编号不重置**：短篇合集约 25-30 句，全书排序；不同篇可以有相同的"原句 N"但金句编号全体系唯一。
3. **情感节点与金句独立编号**：两者都用圆圈数字但属不同文件，不可混淆。
4. **审查方自编号 ≠ 文件章节号**：审查报告中的"⑩"是审查方为方便引用而自编的序号，**不对应任何文件中的章节号**。发现缺陷后必须先读文件定位，再 grep 原文确认——禁止直接套用报告暗示的章节映射。

   **实证**：Aickman 审查报告中"⑩=ch01"、"⑬=ch04"——⑩ 对应金句精选第 10 句（ch01 的引文），而非文件中的 ch10；⑬ 对应第 13 句（ch04 的引文），而非 ch13。

5. **COLLABORATION.md 中引用格式**：缺陷定位必须包含文件名 + 实际章节 + 引文内容片段，禁止只写"金句⑩有问题"而不说明具体文件和章节。
   - ✅ `金句精选.md ⑩（ch01 第 1 句）`
   - ❌ `金句⑩`

### 其他易混淆规则

| 规则 | 常见混淆 | 正确做法 |
|------|---------|---------|
| verify_quotes 主口径 | 以为总览层也受其保护 | verify_quotes 只管 `chNN.md`，**不覆盖** `00_*.md`（总览三篇） |
| check_vocab FAIL=0 | 以为词汇表已干净 | 工具只检词频，不识别中文标注；Barron's 批次 FAIL=0 时仍有 928 条 A 类虚构；**Ligotti ch33 nullify/ch52占位符/ch73 night 在别章出现即过 FAIL，但例句是虚构** |
| check_vocab 逐章验证 | 以为全书词频口径够用 | **必须逐章 grep 验证例句**，不能只看 FAIL 数字——A 类虚构在书的其他章出现，check_vocab 不会报 FAIL |
| check_chapter_quotes.py | 以为只针对 100 Great | AGENTS.md 第 9e 条：**凡有 text/ 的书一律加跑**，不设例外；**Ligotti ch22 PLACE heading** `"the diseased waters await his embrace"` flat 匹配失败（原文有 whose），属工具假 MISS，非引语虚构 |
| check_chapter_quotes flat 匹配 | 以为 MISS 都是引语虚构 | 工具按 `re.sub(r'[^a-z0-9]', '', s.lower())` 规范化后匹配，省略号/标点差异导致假 MISS；**以 `grep -i "phrase" text/chNN.txt` 人工确认为准** |
| "引语逐字" | 只验证引语行，不验分析/词汇层 | 第 9 条修复同步规则：引语换新句时分析/关键词/中文理解全须同步重写 |
| 省略号截断 | 以为只要有 `…` 就是合法截断 | `…` 左侧必须是原文原词，**禁止在右侧添加原文不存在的词**（gilding） |
| git add -A | 多 IDE 并行时方便 | **禁止** `git add -A` / `git add .`——只 add 明确路径清单 |
| 旧格式圈数字转换 | 以为转换后自动编号正确 | **Ligotti 49 章转换后 ch22/ch27/ch28/ch29 全部残留 `原句1`**（映射逻辑 `①→1` 后对所有圈数字重复执行替换）；转换后必须检查编号是否连续递增 |

---

## 独立审查五步法（审查方专用）

执行方交付审查时，必须先确认以下材料齐全：

1. **三件套原始输出**——verify_quotes / check_vocab / check_chapter_quotes 原始逐行输出
2. **总览自检声明**——概述/金句集/情感节点引语逐句 grep 结果（MISS=0）+ 人物身份/关系/结局的原文支撑
3. **跨书污染自检**——不熟悉的人名/地名先 `grep -rl "<name>" notes/books/` 排除

审查方五步：
- a. **三件套重跑**：重跑 verify_quotes + check_vocab + check_entities，**不信报告数字**——NS 报告 101/101 实为 108/109；TN 报告 44/44 含 1 处跨章错植未报
- b. **逐章归属校验**：逐篇跑 check_chapter_quotes.py，**凡有 text/ 的书一律加跑**（AGENTS.md §9e 不设"100 Great 专用"例外），必须全部"X/X in chNN text"，零跨章搬句；cliffhanger 跨章边界须同时读前后章 text/ 各 ~100 行确认实际发生章节
- c. **结构扫描**（行首引语块口径）：`grep '^> \*\*原句' chNN.md`，扫编号连续 / 四件套齐全 / 零孤儿块 / 零重复块；禁止用整文字符匹配（会把导航里的圈数字引用也算进去）
- d. **语义二审**：引语↔分析逐对核对；委派子代理时**必须附 1-2 个本库真实失败案例 + 防幻觉条款**（禁止拿 text/ 句子与无关分析行拼装错位；Golden Boy / In a Heartbeat / A Real Paige Turner 三书累计 6 处实证）；子代理额度耗尽时主会话自审不可省
- e. **总览层事实核对**：概述/金句精选/情感节点引语**逐字 grep epub**，命中后须 grep 前后 ~200 字符窗口确认说话人（逐字命中 ≠ 说话人正确；Room 实战：㉒ "What I did to you" 逐字命中但说话人 Rudbeck→Kim）
   - **验证脚本优先级**：逐篇 `find()` 原样匹配 epub 原文 > normalize 拼接脚本 > 审查报告数字。normalize 拼接（去标点/去引号）对含 `'`/`’`/超长句有系统性假 MISS——Aickman 金句 9 句被脚本报 MISS，逐篇 find 后全部命中 epub，属工具误报；凡工具报 MISS 须先用逐篇 find 复核，确认真 MISS 才改

---

## 终验快速检查清单（commit 前逐项划 ✓）

- [ ] verify_quotes 全绿且原始输出已贴
- [ ] check_vocab FAIL=0 WARN=0 且原始输出已贴
- [ ] check_entities 0 未知实体
- [ ] check_chapter_quotes 逐章 X/X in chNN text（有 text/ 时）
- [ ] verify_overview_quotes 全绿（有总览时）
- [ ] check_crossref 报警已人工复核
- [ ] 短引语 <20 字符已逐条人工 grep
- [ ] 关键词锚定已自查（≥20 章的书）
- [ ] 词汇表无占位行（grep '\| *$' 无输出）
- [ ] git pull + status 确认工作树未被他人提交

---

## 版本历史

- 2026-09-07：新增 index.lock 处理、re.S 禁区、关键词锚定检查器、check_crossref 输出解读、快速检查清单、门禁时序表常见错误列
- 2026-09-06：新增门禁时序表、工具盲区速查、22 条跨书互证坑、check_crossref 工具、verify_quotes 言情口径
- 2026-08-31：新增协作板身份/时间戳规范、编号规则防混淆
- 2026-08-29：新增内联 Gate、引语修复同步、独立审查 SOP
- 2026-08-27：新增 verify_quotes 门禁
- 2026-08-26：初版（格式定义 + git 策略）
